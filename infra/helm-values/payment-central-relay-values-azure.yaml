
redis:
  url: redis://redis-master:6379
kafka:
  bootstrapServers: kafka.payment.svc.cluster.local:9092
spring:
  profile: azure
serviceMonitor:
  enabled: false
  labels:
    release: prometheus-stack
  path: /actuator/prometheus
  interval: 30s
  scrapeTimeout: 10s
replicaCount: 1
autoscaling:
  enabled: false
resources:
  requests: {cpu: "1000m", memory: "1024Mi"}
  limits: {cpu: "3000m", memory: "2048Mi"}
