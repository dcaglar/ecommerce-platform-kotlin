mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.dogancaglar.paymentservice.adapter.persistence.entity
  type-handlers-package: com.dogancaglar.paymentservice.adapter.persistence.typehandler
spring:
  liquibase:
    enabled: false
