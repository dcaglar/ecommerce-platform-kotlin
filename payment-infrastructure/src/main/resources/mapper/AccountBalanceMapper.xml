<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dogancaglar.paymentservice.adapter.outbound.persistence.mybatis.AccountBalanceMapper">


    <resultMap id="AccountBalanceResultMap"
               type="com.dogancaglar.paymentservice.adapter.outbound.persistence.entity.AccountBalanceEntity">
        <id property="accountCode" column="account_code"/>
        <result property="balance" column="balance"/>
        <result property="lastAppliedEntryId" column="last_applied_entry_id"/>
        <result property="lastSnapshotAt" column="last_snapshot_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>


    <select id="findByAccountCode" resultMap="AccountBalanceResultMap">
        SELECT account_code, balance, last_applied_entry_id, last_snapshot_at, updated_at
        FROM account_balances
        WHERE account_code = #{accountCode}
    </select>

    <select id="findByAccountCodes" resultMap="AccountBalanceResultMap">
        SELECT account_code, balance, last_applied_entry_id, last_snapshot_at, updated_at
        FROM account_balances
        WHERE account_code IN
        <foreach collection="accountCodes" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
    </select>


    <!-- Upsert that only advances if watermark increases -->
    <insert id="insertOrUpdateSnapshot">
        INSERT INTO account_balances
        (account_code, balance, last_applied_entry_id, last_snapshot_at, updated_at)
        VALUES
        (#{accountCode}, #{balance}, #{lastAppliedEntryId}, #{lastSnapshotAt}, #{updatedAt})
        ON CONFLICT (account_code) DO UPDATE
        SET
        balance = EXCLUDED.balance,
        last_applied_entry_id = EXCLUDED.last_applied_entry_id,
        last_snapshot_at = EXCLUDED.last_snapshot_at,
        updated_at = EXCLUDED.updated_at
        WHERE account_balances.last_applied_entry_id  &lt;  EXCLUDED.last_applied_entry_id
    </insert>


    <select id="findAll" resultMap="AccountBalanceResultMap">
        SELECT account_code, balance, last_applied_entry_id, last_snapshot_at, updated_at
        FROM account_balances
        ORDER BY account_code
    </select>

</mapper>

