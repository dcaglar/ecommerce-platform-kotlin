<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="20260208-optimize-idempotency-performance" author="dogancaglar">
        <preConditions onFail="MARK_RAN">
            <tableExists tableName="idempotency_keys"/>
        </preConditions>

        <dropPrimaryKey tableName="idempotency_keys" constraintName="idempotency_keys_pkey"/>

        <addColumn tableName="idempotency_keys">
            <column name="id" type="BIGINT"/>
        </addColumn>

        <sql>
            CREATE SEQUENCE IF NOT EXISTS idempotency_keys_id_seq;
            UPDATE idempotency_keys SET id = nextval('idempotency_keys_id_seq') WHERE id IS NULL;
            ALTER TABLE idempotency_keys ALTER COLUMN id SET DEFAULT nextval('idempotency_keys_id_seq');
            ALTER TABLE idempotency_keys ALTER COLUMN id SET NOT NULL;
        </sql>

        <addPrimaryKey tableName="idempotency_keys" columnNames="id" constraintName="pk_idempotency_keys_id"/>

        <addUniqueConstraint tableName="idempotency_keys" columnNames="idempotency_key" constraintName="uk_idempotency_key"/>

        <sql>
            ALTER TABLE idempotency_keys SET (
            fillfactor = 70,
            autovacuum_vacuum_scale_factor = 0.01,
            autovacuum_vacuum_cost_limit = 1000
            );
        </sql>

    </changeSet>

    <changeSet id="20260208-optimize-idempotency-vacuum" author="dogancaglar" runInTransaction="false">
        <sql>
            VACUUM FULL idempotency_keys;
        </sql>
    </changeSet>

</databaseChangeLog>