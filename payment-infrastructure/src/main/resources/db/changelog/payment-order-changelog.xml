<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="2025-11-11-create-payment-orders" author="dogancaglar">

        <!-- 1️⃣ Table definition -->
        <createTable tableName="payment_orders">

            <column name="payment_order_id" type="BIGINT">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="payment_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_payment_orders_payment"
                             referencedTableName="payments"
                             referencedColumnNames="payment_id"/>
            </column>

            <column name="seller_id" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="amount_value" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="amount_currency" type="CHAR(3)">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <column name="retry_count" type="INTEGER" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- 2️⃣ Constraints (SQL for cross-version Liquibase compatibility) -->
        <sql>
            ALTER TABLE payment_orders
            ADD CONSTRAINT chk_payment_orders_status_valid
            CHECK (status IN (
            'INITIATED_PENDING',
            'CAPTURE_REQUESTED',
            'CAPTURE_FAILED',
            'CAPTURED',
            'REFUND_REQUESTED',
            'REFUND_FAILED',
            'REFUNDED'
            ));
        </sql>

        <sql>
            ALTER TABLE payment_orders
            ADD CONSTRAINT chk_payment_orders_currency_3
            CHECK (amount_currency ~ '^[A-Z]{3}$');
        </sql>

        <sql>
            ALTER TABLE payment_orders
            ADD CONSTRAINT chk_payment_orders_amount_positive
            CHECK (amount_value > 0);
        </sql>

        <sql>
            ALTER TABLE payment_orders
            RESET (autovacuum_enabled);
        </sql>


        <!-- 3️⃣ Useful indexes -->
        <createIndex indexName="idx_payment_orders_payment_id"
                     tableName="payment_orders">
            <column name="payment_id"/>
        </createIndex>

        <createIndex indexName="idx_payment_orders_seller_id"
                     tableName="payment_orders">
            <column name="seller_id"/>
        </createIndex>

        <!-- 4️⃣ Rollback -->
        <rollback>
            <dropTable tableName="payment_orders"/>
        </rollback>

    </changeSet>

</databaseChangeLog>