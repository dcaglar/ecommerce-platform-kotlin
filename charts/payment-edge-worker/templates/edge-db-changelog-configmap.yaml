apiVersion: v1
kind: ConfigMap
metadata:
  name: edge-db-changelog-configmap
data:
  # "db/" kısmını anahtarlardan temizler, sadece dosya adını bırakır.
    {{- range $path, $bytes := .Files.Glob "db/*" }}
    {{ base $path }}: |-
  {{ $bytes | toString | indent 4 }}
  {{- end }}