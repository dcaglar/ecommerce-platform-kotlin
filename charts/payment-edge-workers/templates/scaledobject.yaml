{{- if .Values.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "payment-edge-workers.fullname" . }}-scaler
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "payment-edge-workers.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    name: {{ include "payment-edge-workers.fullname" . }}
    kind: StatefulSet
  minReplicaCount: {{ .Values.keda.minReplicaCount | default 1 }}
  maxReplicaCount: {{ .Values.keda.maxReplicaCount | default 2 }}
  pollingInterval: {{ .Values.keda.pollingInterval | default 15 }}
  cooldownPeriod: {{ .Values.keda.cooldownPeriod | default 300 }}
  triggers:
    {{- toYaml .Values.keda.triggers | nindent 4 }}
{{- end }}
